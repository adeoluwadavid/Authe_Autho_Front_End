export default class User {
    constructor(firstName, lastName, username, address, email, password) {
        this.firstName = firstName,
        this.lastName = lastName,
        this.address = address
        this.username = username;
        this.email = email;
        this.password = password;
    }
}